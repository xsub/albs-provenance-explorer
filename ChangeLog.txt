ChangeLog
=========

Chronological log of implementation progress and fixes on the `max` branch --
the *how* of what shipped. Paired with `docs/decisions.md` (the *what* and
*why* of each architectural / contract decision): every entry here names the
commit hash and the matching D-entry or F-finding so a reader can follow
either thread.

Conventions:
  - Newest at the top.
  - One bullet per commit; short hash at the start; D-entry / F-finding at end.
  - Pre-F1 history (D1-D44 and the original `main` branch commits) is not
    duplicated here; those choices are captured in `docs/decisions.md` and the
    corresponding code landed before this changelog was instituted. Walk it
    via `git log main --oneline` if needed.

----------------------------------------------------------------------
2026-05-31
----------------------------------------------------------------------

- (this)   fix: show the log while fetching; errata "checked-clean"; dedup CVE
           edges (D126). run_analysis switches to the Log tab so progress is
           visible. The ERRATA badge is active once errata was *consulted*
           (_errata_consulted), reading "ERRATA: NET (clean)" / "(3)", so a clean
           build no longer looks unfetched. _attach_advisory now adds the
           errata->cve FIXES edge once (guarded by _has_fixes_edge) instead of
           once per RPM -- a CVE node no longer sprouts dozens of identical
           edges. +3 cases. Suite 429 -> 432. decisions.md D126.

- 43001c9  feat(gui): badges -- errata NET/DNF, a CAS badge, an AlmaLinux host
           badge (D125). ERRATA names its source (NET feed / DNF host / NET+DNF
           cross-check); a CAS badge appears only when the `cas` tool is on the
           host and shows the externally-verified count (click -> use_cas); an
           AlmaLinux indicator badge sits last on the right on an AlmaLinux-family
           host. A _baseline_host autouse fixture keeps construction deterministic
           on any CI box. +1 case. Suite 428 -> 429. decisions.md D125.

- 7e8a85a  fix(gui): timeline -- no column overlap + reveal a node on click
           (D124). The tree's long "Stage" labels overflowed into "Status";
           column 0 is now ResizeToContents and text elides (ElideRight).
           Clicking a graph node also locates it in the timeline:
           TimelinePanel.reveal_node selects+scrolls the tree and centres the
           Gantt on the matching row; _graph_node_clicked calls it. +2 cases.
           Suite 426 -> 428. decisions.md D124.

- d71a747  feat(gui): build-list fetch shows live progress in the status bar
           (D123). fetch_recent_builds gains on_progress(fetched, limit);
           refresh_build_list feeds _build_fetch_progress which writes
           "Fetching builds… 30/100 (30%)" + processEvents so it repaints
           mid-loop; a _refreshing_builds guard ignores re-entrant clicks.
           +1 case. Suite 425 -> 426. decisions.md D123.

- 1f902ae  feat(gui): start launcher, verified build-id entry, identifier badges
           (D122). A bare launch opens _StartDialog (Open Saved Session / Inspect
           by ALBS Build ID / by Build ID from list / by ALBS file / by ALBS
           package / offline demo; also File > Start…); Inspect-by-Build-ID takes
           an arbitrary number and verifies it live (fetch_build_summary ->
           name/desc) before enabling Inspect; the badges name their source
           inline (ALBS: <id>, SBOM: <serial/id>, ERRATA: <count>). entry.py
           --source default -> None. +6 cases. Suite 419 -> 425. decisions.md
           D122.

- 3b28d9c  feat(builds): build catalog UX -- a filterable list, time-sorted,
           configurable last-N (D121). Browse is now a _BuildPickerDialog
           (QListWidget with a one-line description per build + an in-place
           filter), the catalog sorts by build time (created_at desc, fallback
           id), and the Builds menu offers Refresh from ALBS > Last 50/100/200/500
           via fetch_recent_builds (pages the 10/page endpoint to N, default
           100). _record_analyzed_build preserves created_at so the sort holds.
           +2 cases. Suite 417 -> 419. decisions.md D121.

- 71ca34c  feat(builds): a cached catalog of real build numbers (D120) so a
           sparse-id guess (a 404) is avoidable. adapters.albs gains
           fetch_build_list/parse_build_list/BuildSummary off the ALBS list
           endpoint; services.BuildCatalog persists them as a JSON db under
           default_cache_root (upsert-by-id, tolerant of missing/corrupt). GUI:
           a Builds menu (Browse Builds... picker + Refresh Build List), a
           QCompleter on the build-id field, and record-on-analysis. Startup
           stays offline. +7 tests (an autouse fixture isolates the cache dir).
           Suite 410 -> 417. decisions.md D120.

- c112d6c  feat(errata): cross-check the web feed against dnf and mark the
           advisories both confirm. A fourth errata mode "both" consults
           errata.almalinux.org *and* dnf updateinfo; attach_errata_cross_checked
           records sources=[...] + a cross_checked flag on each ERRATA node and
           per-RPM FIXES edge (true when >=2 agree, false for a single-source
           discrepancy); a clean both agree on is errata_cross_checked. Degrades
           to one source if the other is unreachable. GUI: combo "both
           (cross-check)", Security "Errata" cell gets a [x-checked] suffix,
           badge tooltip + inspector show it. Three-state (D79) untouched.
           +5 tests. Suite 405 -> 410. decisions.md D119.

- ef2fe64  fix(gui): a missing build is "not found", not "Analysis failed". A
           sparse-id 404 (ALBS ids are not sequential: 57809 does not exist,
           57810 does) popped a red failure. fetch_build_metadata now raises a
           dedicated BuildNotFoundError (a ValueError subclass); the worker
           routes it to a build_not_found signal and _build_not_found shows a
           calm "Build N not found" status + an info dialog (check the id on
           build.almalinux.org), keeping the previous result. +2 GUI tests; the
           albs 404 test now asserts the type. Suite 403 -> 405. decisions.md
           D118.

- 77e9922  feat(gui): errata default is host-aware -- dnf on AlmaLinux, http
           elsewhere. A fetch-all that turns errata on now picks
           _default_errata_source(): dnf (local updateinfo, authoritative) on an
           AlmaLinux/RHEL-family host with dnf, the errata.almalinux.org HTTP
           feed otherwise (e.g. macOS). _select_default_errata respects an
           explicit combo choice; the ERRATA badge tooltip reads "dnf
           updateinfo (host)" when dnf is picked. +1 GUI test. Suite 402 -> 403.
           decisions.md D117.

- b467044  feat(gui): the primary Analyze action fetches all for a build id.
           The sparse result a user hit came from the toolbar Analyze button
           (Ctrl+R) running a plain run_analysis (errata off, no enrichment) --
           only Enter did the fetch-all. Analyze is now context-sensitive
           (_analyze_or_fetch_all): a live build id -> fetch every host source;
           a cached source file -> a plain offline run (no surprise network).
           _select_errata_http now respects an explicit errata choice (only
           off -> http). +1 GUI test. Suite 401 -> 402. decisions.md D116.

- d54bd41  feat(gui): a build-id fetch-all pulls every host-available source
           in-app. Enter sets a one-shot _deep_fetch that _run_spec merges into
           the RunSpec -- RPM headers always, dnf/sonames + cas only when the
           host tool exists (shutil.which) -- on top of the ALBS base, errata
           (http) default and SBOM autodiscovery, so one keystroke fetches all
           the host can. The heavy full-RPM rungs (payloads/ELF, signature
           checksig) and SBOM generation stay in Run Full Inspection (run.sh).
           Also: the Timeline Tree/Gantt switch no longer renders clipped to
           "Ga" (min width); the README workbench screenshot caption was
           refreshed and the stale "live errata fetch is future" line fixed.
           +2 GUI tests. Suite 399 -> 401. decisions.md D115.

- 505579f  feat(gui): interactive, cache-aware source badges. The status-bar
           ALBS/ERRATA/SBOM badges are now persistent clickable buttons that
           grey out (amber=stale cache, grey=missing) when a source's data is
           absent or older than the TTL for the current build id; clicking one
           fetches just that resource, while build id + Enter fetches them all in
           one sweep. ALBS state reads the metadata cache (_cache_file_state)
           under the run.sh OUT_DIR convention, which _load_spec now writes; a
           click forces a refetch. Supersedes the static badges of D110; drops
           the dead _last_load_spec/_last_run_spec. +1 net GUI test. Suite
           398 -> 399. decisions.md D114.

- a5adab3  feat(gui): File > Inspect Binary (RPM) -- pick a local .rpm and run
           the CLI inspect-rpm on it (package/provide/require + header facts) in
           the subprocess console dialog. Enabled only on an AlmaLinux /
           RHEL-family host with rpm (_is_almalinux_family_host checks
           shutil.which("rpm") + /etc/os-release ID/ID_LIKE against
           {almalinux,rhel,centos,rocky,fedora,el}); greyed out with a tooltip
           elsewhere. +1 test. Suite 397 -> 398. decisions.md D113.

----------------------------------------------------------------------
2026-05-30
----------------------------------------------------------------------

- 27253e3  docs(tests): add docs/tests.md -- a guide to the 397-test suite
           grouped by type (graph model, ALBS ingestion, RPM analysis, native
           tooling, dependency/reconcile/resolvers, SBOM, security feeds, CAS,
           provenance analyses, universe/store, pipeline/services, render/CLI,
           PyQt GUI), one row per test file with a count + a brief of what it
           covers, plus the offline-only principle and how to run. No code.

- 2c810ae  fix(gui): graph nodes/edges are now reliably clickable. The cmapx
           image map rendered at ~96 dpi (regions to ~2496x704) while the SVG is
           72-dpi points (viewBox 1897x554), so the click transform (in SVG
           space) only overlapped the ~1.3x-larger hit regions near the top-left
           -- "barely reacts". _run_dot now takes extra dot flags and the cmapx
           is rendered with -Gdpi=72, so the regions share the SVG point space
           (25..1872 x 25..528 inside 1897x554) and every node/edge hits. +1
           graphviz-gated test (all region coords within the SVG viewBox). Suite
           396 -> 397. decisions.md D112.

- d9d06e2  fix(albs+run.sh): report a missing build plainly + run.sh report
           flags. fetch_build_metadata special-cases a 404 -- "Inspect Build Id"
           for a non-existent build (e.g. 57809) now raises the API's own
           "...is not found" detail instead of falling through to the HTML
           fallback and dying with "HTML fallback did not contain build
           metadata"; a valid build (57810) fetches fine. +1 offline test. Also,
           live el10 run.sh exposed report-flag bugs: license needs
           --rpm-licenses, and the downstream reports are now best-effort
           (|| echo continuing) so one failure (license) no longer aborts slsa.
           Suite 395 -> 396. decisions.md D111.

- c503208  fix(run.sh): correct the CLI output flags. coverage prints its report
           to stdout (no --format/--output) and writes the metadata cache via
           --cache; vuln/license take --format only (no --output); slsa takes
           --output. run.sh now pipes coverage/vuln/license through tee (shown in
           the GUI subprocess dialog + saved) and passes slsa --output. Verified
           the el10 run reached the coverage rungs. No test change (shell).

- c0cbebe  feat(gui): run.sh full-inspection template + status-bar source badges
           + Inspect-Build-Id menu. (1) New top-level run.sh <build_id> runs a
           complete inspection pulling from every source the host supports
           (ALBS fetch, coverage with headers/payloads/dnf/sonames/sigs +
           errata http + optional CPE, then vuln/license/slsa), writing JSON to
           OUT_DIR; only the build id is required. The workbench's "Run Classic
           Pipeline" becomes "Run Full Inspection (run.sh)" -> bash run.sh <id>
           from this repo (drops the dead _classic_root/_ClassicRequest max-
           checkout machinery). (2) After a run, status-bar badges (ALBS/SBOM/
           ERRATA/DNF/SIG/CPE) show which external sources were contacted, with
           the resource URI on hover. (3) Run > Inspect Build Id... prompts for a
           build id and analyses it in-app. +2 GUI tests. Suite 393 -> 395.
           decisions.md D110.

- 0c6aead  fix(gui): a mismatched build SBOM no longer blocks analysis + errata
           combo readability. A build id of 57812 with a stale 57810 Source/SBOM
           raised "Build SBOM appears to be for build 57810..." as a modal that
           stopped the whole run, so flipping the errata toggle + re-analysing
           never happened (the errata-less result stayed). The SBOM is auxiliary:
           _autofill_build_sbom now re-discovers the right build-<id>.cyclonedx
           when the current one is for a different build (drops the stale one),
           and _run_spec drops a surviving mismatch with a log note instead of
           raising. Also the errata-source combo was too narrow to read its
           selection -- items shorten to off / http (almalinux.org) / dnf (host)
           with a min width (the "Errata" label gives context). +1 GUI test.
           Suite 392 -> 393. decisions.md D109.

- 821ecb5  feat(errata): make the live errata.almalinux.org fetch work + default.
           "Errata: missing" is the not_checked default; pointing the D79 http
           source at the real feed exposed three blockers, all fixed: (1)
           errata_source=http with no feed/URL now defaults to the canonical
           AlmaLinux feed for the build's own distro -- almalinux_major_version
           reads the dominant .elN from RPM releases, almalinux_errata_feed_url
           builds errata.almalinux.org/<N>/errata.full.json; (2) the feed 403s
           the default Python-urllib agent, so _default_http_get now sends a
           descriptive User-Agent; (3) macOS framework Python has no CA bundle,
           so the fetcher uses a certifi-backed ssl context (certifi declared
           explicitly), system store on Linux. Plus errata now scopes to ALL
           arches (the default x86_64+noarch selector left ppc64le/aarch64 RPMs
           not_checked). Verified live (build 17812 el9): all 90 RPMs consulted ->
           confirmed_clean. Our _index_feed already parsed the real format. +4
           test cases. Suite 388 -> 392. decisions.md D108.

- 6665efc  fix(gui): workbench usability regressions (toolbar overflow, Enter,
           file load). (1) The M3 security inputs (errata/CPE/CVE) overflowed the
           single toolbar into Qt's >> extension menu; they move to a second
           "Security sources" toolbar row (addToolBarBreak), Source/SBOM shrink,
           and the primary row leads with explicit Open + Analyze buttons. (2)
           build_id Enter shelled out to the classic example--full.sh subprocess
           (D88) which dies off an AlmaLinux host; Enter (build_id + source) now
           runs the in-app analysis, and the classic runner is an explicit Run >
           Run Classic Pipeline menu action. (3) Loading a build JSON did not
           populate artifacts (a stale build id shadowed the source and
           open_source only filled the field); open_source now clears the build
           id and auto-runs, and Enter-in-Source drops a stale build id so the
           file wins. No parse bug (a cached build loads its real 90 RPMs). +2
           GUI regression tests. Suite 386 -> 388. decisions.md D107.

- 9972ae0  refactor(gui): retire the blanket # mypy: ignore-errors on qt_app.py
           (quality follow-up; the GUI is now fully strict-typed). Deleting the
           blanket ignore surfaced 122 real errors (PyQt5 5.15 ships .pyi stubs);
           all fixed by category, NOT 122 per-line ignores: ~40 flattened-enum
           attr-defined -> scoped enums (Qt.UserRole -> Qt.ItemDataRole.UserRole,
           QStyle.SP_* -> QStyle.StandardPixmap.SP_*, ...); 54 union-attr -> one
           generic _require(x: T|None)->T narrower wrapped around the Optional Qt
           accessors (menuBar/horizontalHeader/style/statusBar/globalInstance/
           widget(i)/item(i)/view()/renderer()/viewport()); 6 override -> nullable
           event-handler args + guards; 12 RunSpec(**dict[str,object]) arg-type ->
           the kwargs helpers return dict[str, Any]; the classic-runner
           dict[str,object] -> a _ClassicRequest TypedDict; plus findings/param
           annotations + a TextInteractionFlags() construction. EXACTLY ONE
           targeted # type: ignore[arg-type] remains (idiomatic QWidget.close ->
           void signal). qt_app.py type-checks under mypy --strict (strict=true,
           only the zstandard override); no behaviour change, suite stays 386.
           decisions.md D106.

- 22f3777  refactor(gui): extract the Timeline panel into a typed module
           (god-object split #4; quality follow-up). New gui/timeline_panel.py
           bundles the whole build-task timeline: the custom ~100-line
           TimelineGanttView (QGraphicsView), the tree view, the Tree/Gantt
           switch + stack, the recursive tree-item builder, both activation
           paths, and the _format_seconds / _gantt_palette helpers. Host drives
           it with populate(graph, build_analysis, dark=) + one navigate
           callback. First extraction to hit the "targeted, not blanket" half of
           the goal: the Gantt drawing's Optional scene-adds + nullable
           mousePressEvent are fixed with a _scene_text assert-helper + a
           None-guard + a nullable override -- strict-clean with ZERO
           type:ignore. qt_app drops the class + assembly + _populate_timeline
           body + _timeline_item + _timeline_activated + min-height fixups + the
           2 helpers + GANTT_MIN_HEIGHT + 2 imports (2331 -> 2023 lines). No
           behaviour/test-count change (view-switch test repointed at
           window.timeline_panel.view_combo). mypy now checks 89 files. ALL 4
           panels extracted; retiring the blanket ignore on the residual shell
           is a separate follow-up. decisions.md D105.

- 931e5da  refactor(gui): extract the Dependency panel into a typed module
           (god-object split #3; quality follow-up). New gui/dependency_panel.py
           -- a DependencyPanel(QWidget) owning the scope / only-conflicts /
           only-unresolved filters AND the 11-col table; it caches the last
           graph so a filter change re-renders itself (host calls populate(graph)
           once). Session coupling via filters()/restore(); qt_app drops the
           table + 3 filter widgets + the panel assembly + _populate_dependency_
           table body (now a delegate) + _tint_dependency_cell +
           _dependency_activated + _set_dep_scope + the now-unused dependency_rows
           import, and unpacks filters() in _current_session. Typed under mypy
           strict, no ignore. No behaviour/test-count change (3 GUI tests
           repointed at window.dependency_panel.* scope_combo/only_conflicts/
           table/restore). mypy now checks 88 files. 3/4 panels extracted.
           decisions.md D104.

- cbfee8b  refactor(gui): extract the Security panel into a typed module
           (god-object split #2; quality follow-up). New gui/security_panel.py
           -- a SecurityPanel(QWidget) owning the 9-col table + colour-tinting,
           rendering the per-RPM security posture (security_rows) via a single
           populate(graph, cve_feed=) entry + one navigate(node_id) callback. The
           CVE feed stays a host-loaded report-time input (_ensure_cve_feed,
           D101) passed into populate, so the panel is toolbar-free. qt_app drops
           the table + _populate_security_table body + _tint_security_cell +
           _security_activated + the now-unused security_rows import (2393 ->
           2331 lines) and delegates. Typed under mypy strict, no ignore (Qt
           Optional guarded, Qt.ItemDataRole.UserRole). No behaviour/test-count
           change (smoke + D101 Potential-CVEs tests repointed at
           window.security_panel.table). mypy now checks 87 files. 2/4 panels
           extracted. decisions.md D103.

- 83cd2af  refactor(gui): extract the Universe panel into a typed module
           (first god-object split; quality follow-up). New gui/universe_panel.py
           -- a UniversePanel(QWidget) owning the M4 widgets + store + focus +
           favourites + handlers, needing only injected log/show_error callbacks
           and a small session API (store_path/favourites/restore). It
           type-checks under mypy strict with NO ignore (Qt Optionals guarded;
           Qt.ItemDataRole.UserRole), unlike qt_app.py's blanket ignore. qt_app
           drops ~225 lines (3 attrs + 8 methods + the builder) and delegates to
           the panel. No behaviour/test-count change (the 2 universe GUI tests
           repointed at window.universe_panel.*). mypy now checks 86 files.
           Establishes the template to retire the blanket ignore panel-by-panel.
           decisions.md D102.

- 4f648ce  test(gui): deeper qt_app interaction coverage (quality follow-up).
           +3 GUI test cases drive the previously-untested read-only paths --
           timeline Tree/Gantt switch, layer-filter toggle re-render, graph
           search, zoom/fit/reset, graph query run, finding drill-down, recipe
           apply, edge inspect, artifact filter; bundle + HTML export (via a
           monkeypatched save dialog); and save_session writing a loadable file.
           An autouse fixture stubs QMessageBox so no modal hangs the headless
           run. qt_app.py 68% -> 73%. Suite 383 -> 386. limitations.md notes the
           remaining mypy-ignore/god-object split is best done by extracting
           typed panel classes (mypy reports ~150 real errors without the
           blanket ignore).

- 35caa05  feat(workbench): CVE-feed + CPE-verify toggles so the Security
           panel's Potential CVEs column populates (M3, closes the last gap).
           Two coordinated inputs: cpe_dict_edit -> RunSpec.verify_cpe /
           verify_cpe_url (analyze-time, the pipeline VerifyCpeStep resolves an
           official CPE so a feed can match), and cve_feed_edit -> _ensure_cve_
           feed (report-time CveFeed via live_feeds.fetch_cve_feed_or_none, file
           or cached URL, crash-safe, cached by source). _populate_security_table
           passes the feed to security_rows; editing the feed re-renders just the
           panel (editingFinished) without re-analysing. Both persist in the
           session. +2 test cases (backend match incl. out-of-range exclusion;
           GUI verify-cpe kwargs + panel cell). Suite 381 -> 383. decisions.md
           D101. el10 VPS-verified earlier this session (381 then).

- 09381eb  docs(readme): reframe the workbench README as the GUI *version* of
           albs-provenance-explorer (not a screenshot bolted onto the CLI
           README). States the shared-backend fact (CLI runs AnalysisPipeline
           directly, GUI via the services facade -- one engine), lists what the
           GUI surfaces natively, and is honest that parity is partial (license /
           slsa / resolve / inspect-rpm / arch-universe build / live CVE-feed
           remain CLI-only). Records the direction: develop in the CLI first +
           backport to the GUI so the backend stays identical, with the end goal
           of one app exposing the backend through an API for the frontend. No
           code.

- e3debeb  feat(workbench): session + report export -- Markdown, PNG,
           reproducibility (plan milestone M5, completes M2-M5). evidence_bundle
           gains a reproducibility appendix (inputs + graph size + tool/python/
           timestamp, injectable for tests) that rides the JSON bundle and
           renders in both the HTML and a new evidence_report_markdown (GFM
           tables + fenced JSON, no inline SVG). New File>Export>Markdown and
           File>Export>PNG (QtSvg.QSvgRenderer -> QImage). WorkbenchSession now
           persists the M2 dependency filters + M4 universe store/favourites, so
           a saved investigation restores the dependency view and re-opens the
           universe; _current_bundle extracted to de-duplicate the exporters.
           +5 test cases. Suite 376 -> 381. decisions.md D100. Workbench done;
           remaining are quality follow-ups + the M3 CVE-feed toggle.

- 01e7dc9  feat(workbench): Universe panel -- open a SQLite store, search, walk,
           render paths (plan milestone M4). Two new store helpers (sql_search
           label/id substring -> (id,type,label); sql_node_labels resolve a
           path's ids -> labels) feed a read-only services.universe.UniverseStore
           facade (typed UniversePackageRow / UniversePathRow; store opened per
           call so the recursive CTEs stay in SQLite). New "Universe" tab,
           independent of the loaded build: Open Universe + package search +
           selectable focus + Dependencies/Dependents/Reachable walks + Find
           Paths (label-resolved chains, hop count) + a Favourites combo that
           captures (store, search, focus, target) to re-run. Bad file -> logged
           error, never a crash. +2 backend tests + 1 GUI test. Suite 373 -> 376.
           decisions.md D99. Next: M5 session + report export.

- fb3ffa2  feat(workbench): Dependency panel -- reconciled groups + scope/
           conflict filters (plan milestone M2). New
           services.workbench.dependency_rows turns each DEPENDENCY_RESOLUTION
           group (written by reconcile during analysis) into one row: subject +
           coordinate, the verdict (consensus/compatible/conflict/insufficient),
           conflict kinds, cross-distro context, and the scope/linkage/state
           facets aggregated from the member claims (via OBSERVED_AS). New
           "Dependencies" tab with a scope combo + only-conflicts/only-unresolved
           filters over an 11-column table, verdict/conflict/context tinted,
           activate -> navigate to the consuming RPM. "build" maps to spec scope
           buildtime; "static" matches linkage. +1 backend test (consensus/
           version_drift/declared-only + every filter) + a GUI toggle assertion.
           Suite 372 -> 373. decisions.md D98. Next: M4 Universe workbench
           (open a SQLite store, traverse dependents, render paths).

- f0452de  feat(workbench): Security panel -- CPE browser + errata/CVE posture
           (plan milestone M3). New services.workbench.security_rows reuses
           vulnerability_report (F1) for identity/errata/CVE/linkage, keys each
           assessment back to its node by (package, arch), and adds the errata
           three-state (from trust_path_report) + the unverified cpe_candidates
           browser (product/version guesses, [verified] marker) on top. New
           9-column Security tab (Package/Arch/Identity/CPE/Candidates/Errata/
           Addressed CVEs/Potential CVEs/Caveats), colour-tinted, activate ->
           navigate to the node. Potential CVEs needs a live feed (not yet wired
           in the workbench) so it reads `-`; addressed CVEs always populate.
           +1 backend test + a GUI populate assertion. Suite 371 -> 372.
           decisions.md D97. Next M3/M2 step: a CVE-feed toggle (mirroring the
           errata toggle), then the Dependency workbench (claim grouping).

- 580252d  feat(workbench): errata-source toggle so `clean` is reachable from
           the Analyze path (plan milestone M3). A toolbar `errata_combo`
           (off / http / dnf, userData == RunSpec.errata_source) plus an
           `errata_feed_edit`: for http the field is an offline feed file when
           it is an existing path (errata_feed), else a live feed URL
           (errata_url); empty still selects http and degrades to not-consulted.
           _run_spec was refactored so build-SBOM and errata compose on one run;
           the toggle persists in WorkbenchSession. No new pipeline wiring
           (ErrataSourceStep is already in DEFAULT_STEPS). +1 GUI test. Suite
           370 -> 371. decisions.md D96. Next M3 step: the Security panel
           (CPE verified-vs-candidate browser + CVE-feed match explanation).

- b703443  feat(workbench): surface the D79 errata three-state in the evidence
           matrix (plan milestone M3, first slice). evidence_matrix_rows' errata
           column now reads `advisory` (an advisory ships this build),
           `clean` (an errata source was consulted and found none -- a normal,
           complete state) or `missing` (no source consulted), via a new
           _errata_cell over report.errata_status, instead of a bare ok/missing.
           The completeness count is unchanged (present OR clean already counts).
           +1 test (advisory/clean/missing). Suite 369 -> 370. Next M3 step:
           a workbench errata-source toggle so `clean` is reachable live.

- bf51d0b  test(gui): headless smoke test for the workbench main window.
           gui/qt_app.py had 0% coverage (and `# mypy: ignore-errors`); two
           tests now construct WorkbenchWindow under offscreen Qt, drive
           _analysis_finished (populate artifacts/findings/coverage/evidence/
           source/query/timeline/recipes + auto-select + slice render), then
           select an artifact, inspect a node, and cycle every investigation
           mode -- no network, no Graphviz needed (SVG falls back). qt_app.py
           0% -> ~61%. Suite 367 -> 369; docs (plan/limitations/next_goals)
           updated to the new count and the softened "lightly tested" wording.

- ac229e0  docs: surface the workbench in the general docs + flag the
           cross-branch D-number collision. plan.md gains a "Desktop
           investigation workbench" status bullet (services facade + PyQt5 app
           + headless GUI tests). limitations.md gains a "Desktop workbench
           (GUI)" section (qt_app untested/untyped, needs a Qt platform, SVG
           stretch, single-window). next_goals_options.md gains a workbench
           roadmap mapped to plan-pyqt5 milestones M2-M5 + quality follow-ups.
           decisions.md gains a top note that D-numbers above ~D79 diverge
           between this branch and max (match by title, not number). No code.

- 2db3b5a  fix(workbench): address code-review findings in services + gui.
           queries.GraphQueries caches the edge->index map once in __init__
           (O(E)-per-call -> O(1); matters inspecting a large universe).
           compare fingerprint accepts source_rpm OR sourcerpm. slices
           universe_neighborhood drops its dead `depth` arg (one-hop only).
           findings aggregates per-RPM trust gaps into one row per check
           (was ~900 info rows on a 456-RPM build); finding_drilldown_rows
           expands an aggregated finding's affected nodes from metadata.
           gui.qt_app unifies SBOM auto-discovery on the shared
           discover_build_sbom (D78) instead of a parallel re-implementation,
           and closeEvent detaches a running analysis worker's signals so a
           late emit can't target the half-destroyed window. render adds
           colors for source_tree/file/manifest/test_result nodes. ruff +
           mypy clean; 367 pass (one drill-down test updated to the
           aggregated shape).

- 14b2e00  docs(readme): fix the workbench usage examples. The old block led
           with a redundant explicit `--build-sbom` even though the workbench
           already auto-discovers `examples/build-<id>.cyclonedx.json`
           (_autofill_build_sbom). Now: install is its own step, then a few
           described examples starting from the simplest -- `--build-id N`
           (fetch live + auto-find SBOM), no-args (bundled fixture, offline),
           `--source` (re-open already-downloaded metadata), and an explicit
           `--build-sbom` only as a convention override. No code change.

- 13c75a7  ci+docs(workbench): bring CI + badges to the workbench branch,
           PyQt5-aware. New python-app.yml installs `.[dev,gui]` + the Qt
           offscreen plugin's system libs + graphviz, runs ruff + mypy
           --strict + pytest (incl. the GUI tests, headless) + coverage +
           check-test-count on matrix 3.11/3.12/3.13; QT_QPA_PLATFORM=offscreen
           job-wide. New codeql.yml (Scorecard stays repo-level from main).
           tests/conftest.py pins TERM=dumb + COLUMNS=200 (Rich help under CI
           FORCE_COLOR) and QT_QPA_PLATFORM=offscreen (headless Qt) so the
           full 367-test suite runs deterministically with a bare `pytest`.
           pytest-cov added to the dev extra; coverage artifacts gitignored.
           README badge block: CI + CodeQL + Scorecard + codecov (branch-
           scoped) + Built-for-AlmaLinux + PyQt5 + License + Python + Ruff +
           mypy. Verified locally: ruff clean, mypy 84 files, 367 passed
           headless, 68% coverage.

- 6d93c39  docs(demo): refresh examples/demo-build-57810/console.txt from a
           fresh el10 run of example--full.sh (build 57810, nginx-core).
           Reflects the D94 errata three-state in step 1's trust-path table
           -- `has_errata_link | missing (not checked)` instead of a bare
           "missing" -- so the committed demo output matches the branch's
           current backend. Step 11's demo_verbose table keeps the bare
           "missing" (its own renderer). Byte-for-byte from the el10 VPS run,
           same console.txt as max f72abc0 (example--full.sh is CLI-only, so
           the demo output is branch-agnostic). No code change.

- 8dbd8cd  docs(plan): consolidate the PyQt workbench plan doc. The live
           plan-pyqt5-investigation-workbench-app.md is now the merged
           superset -- the 398-line architecture rewrite (Graph Interaction
           Model, query/slice APIs, MVP/Later renderer, Follow-up Milestones
           M2-M5, Data/State Boundaries, Why-PyQt5, Risks, Architectural
           Payoff) PLUS an "Appendix: Current Implementation Status" that
           salvages the older 301-line version's implementation log verbatim
           (Gantt cascade, Evidence matrix, compare_artifacts, cyclonedx
           filename, BuildAnalysis timeline, image-map hit-testing -- the six
           details the rewrite had dropped). Both prior revisions kept verbatim
           as *_deprecated-301.md and *_deprecated-398.md. Concept-coverage
           check confirms all six dropped items are back. Docs housekeeping
           only, no code change. (D95.)

- fc0df31  feat(errata): port the live errata source + three-state errata
           status from max (527f471). The backend port (D92) predated max's
           errata work, so the branch lacked it. Brings adapters/
           errata_source.py (ErrataSource Protocol; HttpErrataSource via
           HttpCache+TTL + DnfErrataSource; both expose consulted:bool),
           TrustPathReport.errata_status three-state (advisory_present /
           confirmed_clean / not_checked; has_errata_link satisfied by
           present OR clean, so a clean package with an SBOM is
           security_context_complete), pipeline ErrataSourceStep + RunSpec
           errata_source/feed/url + CLI flags on coverage/vuln/trust-path.
           Workbench: the Security Context slice + Evidence matrix can now
           distinguish real-advisory from confirmed-clean instead of a
           blanket "errata missing". Cherry-pick clean on the backend
           (incl. cli/main.py); max-numbered doc churn dropped for this
           branch-numbered entry. +15 test cases. Suite 352 → 367. ruff +
           mypy --strict clean. (D94.)

----------------------------------------------------------------------
2026-05-29
----------------------------------------------------------------------

- 15196c1  fix(albs): reject empty ALBS SPA-shell HTML fallback caches. Build
           57811 returns API 404 plus the generic frontend shell at `/build/57811`;
           the old parser cached that as package `AlmaLinux` with zero RPMs,
           making the workbench open a useless 5-node graph. Empty fallback
           pages now raise, older empty fallback caches are discarded before
           refetch, and the workbench's classic runner prefers this checkout
           after the max backend port. Suite now 352. (D93.)

- (this)   feat(backend): port the remaining backend work from max into the
           InvestigationWorkbenchApp branch. Backend file contents now match
           max for the pipeline/CLI, SQLite store, native resolvers, live
           CPE/CVE feeds, arch-universe builder, render helpers, and their
           tests while keeping the PyQt GUI/service layer and project entry
           points intact. This brings over pipeline-backed identify/trust/vuln/
           license, store merge + recursive CTEs + snapshots, pip/Maven/npm
           resolvers, cached live NVD feed fetch, and `arch-universe`.
           Suite now 350. (D92.)

- (this)   feat(cli): port build-SBOM auto-discovery from max. `coverage`,
           `identify`, `trust-path`, and `vuln` now find
           `build-<id>.cyclonedx.json` next to the cache or under examples
           when `--build-id` is used without explicit `--build-sbom`; explicit
           SBOM paths still win and `--no-auto-sbom` disables discovery. Added
           focused discovery/CLI resolver tests; suite now 308. (D91.)

----------------------------------------------------------------------
2026-05-28
----------------------------------------------------------------------

- (this)   docs(README): splice the second (step-11 / demo_verbose) trust-path
           table into the README screenshot so both has_sbom rows read `ok`
           side by side -- visible confirmation of the D72 consistency fix.

- 4ff2cc6  docs: regenerate examples/demo-build-57810/console.txt on the el10
           VPS with the post-fix full.sh. Both trust-path tables (step 1 and
           step 11's inner demo_verbose) now show `has_sbom ok`, resolving
           the inconsistency the user spotted (961 sbom mentions in the demo
           dir alongside a `has_sbom missing` row in the same file). Plus
           ChangeLog placeholder backfill for the bug #8 commit and the
           c6a5e7d demo_verbose fix.

- c6a5e7d  fix(demo_verbose): attach build SBOM so step 11's internal
           trust-path is consistent with the rest of the run. User noticed
           the inconsistency: 961 sbom mentions in
           examples/demo-build-57810/ alongside `has_sbom missing` in the
           same console.txt -- because demo_verbose re-runs trust_path on a
           freshly-built graph and never attached the SBOM. Added
           `--build-sbom PATH` to demo_verbose (calls
           enrich_graph_with_build_sbom after graph_from_build_metadata,
           before the internal trust_path); example--full.sh step 11 now
           passes the existing ${sbom_args[@]}. Suite stays 263. (D72.)

- 4d01655  fix(vuln): split identity_verified into identity_established +
           identity_externally_verified (review bug #8). The single bool was
           true only for NVD-verified CPEs, but CVE matching ran against any
           set CPE including vendor_asserted ones -- the name implied a
           stronger guarantee than the matching behaviour delivered.
           `identity_established` = any CPE set (vendor SBOM or NVD); CVE
           matching can run. `identity_externally_verified` = NVD only.
           CLI render line simplified to `pkg.cpe_status` (the previous
           branch was redundant). Tests updated to the two-field split;
           suite stays 263. Coverage golden byte-identical. (D71; bug #8.)

- 5841c18  fix(source_imports): filter Node stdlib + tag coordinate_kind
           (review bug #7). JS / TS: `require("fs")` / `import "path"` is the
           Node runtime's stdlib, not an npm package -- now filtered (Node 20+
           canonical set; submodules like `fs/promises` collapse via npm_root
           and are filtered too). Java / C / C++: the extractor recovers a
           class path / header path, not a package coordinate -- claims now
           carry `raw["coordinate_kind"]` so a CVE matcher / resolver cannot
           mistake them for Maven groupId:artifactId / system package names.
           Per-language Ecosystem unchanged. +1 net test (tightened JS test
           + new mixed-tree integration verifying coordinate_kind per lang).
           Suite 263. (D70; bug #7.)

- 3e5d36a  fix(reconcile): canonicalise PURL qualifier order in
           IdentityMismatchRule (review bug #4). The rule compared PURLs as
           raw strings, so `...?arch=x&distro=el10` and `...?distro=el10&arch=x`
           tripped a false IDENTITY_MISMATCH even though they are the same
           identity per the PURL spec. New `canonical_purl` helper splits
           on `?`/`#`, sorts qualifiers, re-emits; the rule canonicalises
           before comparison. Scope: qualifier order only (URL-encoding +
           scheme case still raw-compared until a full PURL parser is
           wired in; documented in limitations.md). +2 regression tests
           (order-equivalent does not trip; real value difference still
           does; canonical_purl edge cases). Suite 262. (D69; bug #4.)

- 6f1d6ce  fix(reconcile): claim node ids key on context + PURL (review bug
           #3). `claim_node_id` keyed on subject + coordinate + evidence +
           version, but `_group_key` already keyed on context too -- so two
           claims for the same subject/name/version/evidence with different
           DependencyContext (e.g. arch=x86_64 vs arch=aarch64) collided on
           the *id* even though the reconciler would have put them in
           separate groups. `add_dependency_claim` raised "Conflicting node
           definition" for the second. Now the id includes context_key + PURL
           (when set), matching what grouping distinguishes. +1 regression
           test; coverage golden byte-identical; suite 260. (D68; bug #3.)

- a2c3a18  fix(reconcile): idempotent reconciliation -- safe re-runs on a
           persisted/re-enriched graph (review bug #2). Added minimal
           `ProvenanceGraph.remove_node` + `remove_edges_where` (maintain
           type and adjacency indexes), and a `_purge_prior_reconciliation`
           helper that drops every DEPENDENCY_RESOLUTION node + every
           CORROBORATES / CONFLICTS_WITH edge before rebuilding. Claim nodes
           are preserved (they are reconciliation's inputs). A second run no
           longer duplicates edges; a re-run after a new conflicting claim
           no longer raises "Conflicting node definition for dep-res:...".
           +2 regression tests; coverage golden byte-identical; suite 259.
           (D67; review bug #2.)

- 87cc18d  fix(adapters): HttpCache cannot store Range-ignoring 200 responses
           (review bug #6). `_requests_range_fetch` now requires 206 or 200
           with `len(body) == end - start + 1`; otherwise raises
           `RpmHeaderFetchError` so `_try_candidates` advances to the next
           mirror and HttpCache never sees the bad body. The CDN-cached
           200-with-correct-length case is still accepted (the bytes are
           correct). +1 regression test (mocked Range-ignoring server: the
           call raises and the cache dir stays empty). Suite 257. (D66;
           review bug #6.)

- 227b997  fix(model+security): dry-run isolation + EvidencePatch capture for
           CPE verification (review bug #1). `ProvenanceGraph.copy()` now
           deep-copies node/edge metadata trees, so an in-place mutation on a
           nested value of the copy (`security_identity`, candidate dicts) no
           longer leaks back into the source. `verify_graph_cpe` works on a
           `deepcopy(identity)` per node and routes the final state through
           `graph.update_metadata(...)`, so `RecordingGraph` captures the CPE
           change in `EvidencePatch.metadata_updates`. +2 regression tests;
           coverage golden byte-identical; suite 256. (D65; review bug #1.)

- fd53019  chore: institute ChangeLog.txt + document the convention in
           CLAUDE.md + AGENTS.md (paired with the existing "Keep `docs/` in
           sync" rule). Seeded with the full max-branch history since the
           F-series external review. Pre-F1 history referenced through
           `decisions.md` rather than duplicated.

- 331fb44  Docs refresh: `next_goals_options.md` G1 now marked Done with the
           cache + concurrency story (D63/D64); the Recommendation block was
           still listing B3/C1/F2/F3/G as "remaining" though each is ✅ a few
           lines above -- rewritten to reflect what actually shipped vs what
           is genuinely open (E1 pip/Maven/npm, G2 incremental store, G3
           sqlite-vec, live feeds, live arch builder). `plan.md` §7 "batch +
           parallelize header/payload/SBOM fetches" updated: header/payload/
           sig done, SBOM batching remains. Suite unchanged at 254.

- 6615236  feat(net): wire `rpmsig` through `HttpCache` + max_concurrency.
           `verify_graph_signatures` joins the shared-by-URL cache used by
           `rpm_payload`, so a run with both `--with-rpm-payloads` and
           `--verify-signatures` downloads each RPM once -- payload ELF
           analysis and `rpmkeys --checksig` read the same bytes from disk.
           Same worker-pool / main-thread-merge pattern. Behaviour preserved
           (suite still 254). (D64; #8 follow-up.)

- 3346bf1  feat(net): content-addressed HTTP cache + bounded concurrency.
           New stdlib-only `albs_graph/adapters/_http_cache.py` (key =
           sha256(url + ":" + range), bucketed; atomic writes; 4xx/5xx
           propagate so the mirror cascade still self-heals). Wired into
           `rpm_remote` (default-on header cache) and `rpm_payload` (opt-in
           `--cache-payloads`, since payloads are 5-50 MB). Bounded
           concurrency via `ThreadPoolExecutor(max_workers=4)` in both;
           workers compute pure results, main thread merges (graph never
           mutated off-thread). RunSpec + CLI: `--max-concurrency`,
           `--http-cache/--no-http-cache`, `--cache-payloads`. Coverage
           golden output byte-identical; VPS verify: cold 1230 ms vs warm
           740 ms on a single RPM; 254 tests pass on el10. +8 tests. (D63;
           review item #8.)

- 52c5733  Docs(README): splice the real step-8 output from the post-fix
           VPS run -- the editorial `[output omitted ...]` note replaced
           with the real `Checked out nginx source @ <commit>` + Source
           Evidence table + Source imports lines. No fabrication.

- fe841ad  Docs: regenerate `examples/demo-build-57810/console.txt` on the
           el10 VPS with the post-fix `full.sh` (step 8 now runs cleanly:
           35 files / 1 spec / 48 deps parsed; the ALBS source repo is RPM
           packaging so `source_imports.py` honestly finds 0 .c files --
           it would scan upstream nginx C source if pointed there).
           Committed from the Mac after scp from VPS.

- 2305893  fix(source): idempotent dep nodes -- regression from the first
           real VPS run. A real spec lists the same `Requires:` across many
           subpackage blocks (nginx-core + every nginx-mod-* each
           `Requires: nginx(abi) = %{nginx_abiversion}`); the second
           `add_node` raised "Conflicting node definition for
           dep:rpm:nginx(abi)_=_%{nginx_abiversion}". Made
           `_add_dependency_spec` idempotent: the dep is one node; the
           *edge* from each declaring source file is what multiplies. +1
           regression test; suite 246. (D62.)

- 85c1520  Docs: cross-check + correct doc-vs-impl drift (B follow-up to
           the source-imports build A). Adapter list in CLAUDE.md /
           AGENTS.md went from 6 to all 15 adapters (cas/dnf/elf/pylang/
           rpm_header/rpm_payload/rpm_remote/rpmgraph/rpmsig were
           missing), grouped by role. CLI command list from 10 to all 17.
           `limitations.md` heading "Rungs not yet implemented" renamed
           to "Rung-by-rung implementation status" (Rung 4 is implemented
           with caveats, per the body). `plan.md` "What's done" + README
           "Implemented" gained the multi-language scanner bullet.

- e8b318e  feat(source): multi-language source-tree import/include scanner
           (`albs_graph/adapters/source_imports.py`). Detection by
           extension (.py/.go/.rs/.c/.h/.cpp/.js/.ts/.java/.rb + variants)
           with shebang fallback. Per-language regex extractors anchored
           at line start so a `//` comment cannot match; stdlib filtered
           per language; project-internal references excluded
           (`require_relative`, `./relative` JS imports, `self::`/`super::`/
           `crate::` in Rust). Walker prunes `.git`/`node_modules`/`vendor`/
           `target`/`build`/`dist`/... in-place; bounded by `file_limit`
           (default 5000). Claims dedup per (language, import); each
           carries the language-appropriate `Ecosystem` and a
           `<language>_import` evidence tag. Wired into `source-evidence`
           default-on (`--no-scan-imports` opts out). +12 tests; suite 245.
           (D61.)

- 8c177fc  fix(demo): correct `example--full.sh` step 8 (real bug surfaced
           by the first VPS run): `checkout-source` wants the *source*
           package (nginx, not the binary nginx-core) + a required
           `--dest`; `source-evidence` takes the checked-out dir as a
           positional `SOURCE_DIR`. Added `SRC_PACKAGE` env (default
           nginx). README text-screenshot refreshed to the 14-step run --
           hostname sanitized, the two giant build-intel tables truncated
           with pointers to `console.txt`, step 8 shown as an editorial
           note since the capture predated the fix (nothing fabricated).
           (D60.)

- 2905cc7  chore(demo): consolidate to one comprehensive script
           (`example--full.sh`). Removed `example.sh` / `example--tour.sh` /
           `example--verbose.sh` / `example--almalinux*.sh`; CLAUDE.md /
           AGENTS.md / README references updated. The single script wraps
           all 17 commands end to end. (D59.)

- 91dafa4  feat(tour): close the security-context axis -- wired the real
           committed build SBOM into the tour (closes has_sbom + lifts
           identity to 1.00); added `--errata`/`--errata-subject` to the
           `trust-path` command (parity with vuln/coverage) so the trust
           path's `has_errata_link` can close too; portable empty-array
           idiom `${arr[@]+"${arr[@]}"}` so the script runs on bash 3.2
           too. +1 CLI test; suite 233. (D58.)

----------------------------------------------------------------------
2026-05-27
----------------------------------------------------------------------

- 59f53f5  refactor(cli): extract `AnalysisPipeline`; migrate `coverage`
           onto it. New `albs_graph/pipeline.py`: RunSpec, EnrichmentStep
           Protocol + 13 step objects, DEFAULT_STEPS (exact historical
           coverage order), `AnalysisPipeline.run(spec, graph, *,
           dry_run=)` returning PipelineResult (enriched graph + per-step
           results by name + reconciliation + cumulative EvidencePatch via
           RecordingGraph). `coverage_command` builds a RunSpec, runs the
           pipeline, and aliases `result.result(name)` back to the existing
           render variables -- rendering is unchanged. Verified by
           byte-identical coverage golden output on the synthetic fixture.
           +5 pipeline unit tests (mechanics not exercised by golden);
           suite 232. (D57; review item #1 -- coverage migrated.)

- 6f41e4c  feat(reconcile): IdentityMismatch rule. Fires when 2+ claims in
           a group assert the same concrete version with different PURL
           coordinates -- two sources disagree on *what* the dep is, not
           merely its version. Conservative: only on >=2 PURL-bearing
           claims (the common single-PURL group never fires); grouped by
           version so a real version disagreement is left to
           `VersionDriftRule`. Fills the documented IDENTITY_MISMATCH gap.
           +3 tests; suite 227. (D56.)

- 11a6455  feat(model): EvidencePatch + RecordingGraph. `RecordingGraph`
           wraps a `ProvenanceGraph` (shared state) and records writes to
           a typed `EvidencePatch`; works in apply-and-record mode
           (default) and dry-run when constructed over a copy. Sets up
           "what would this run change?" UX + per-step patches for the
           pipeline. (D55; review item #6.)

- edbea36  Docs: prune stale roadmap / limitations claims (current gaps
           only). Quality pass over `docs/plan.md` and `docs/limitations.md`
           so they describe the system as it now is. (D54; review item #10.)

- 841bf7a  refactor(albs): first-class build/task model -- typed
           BuildTaskRef / BuildSourceRef collections in parsed ALBS
           metadata. Adapters no longer rely on representative-build +
           targeted replacements. (D53; review item #9.)

- c597ec2  perf(graph): index ProvenanceGraph adjacency + node-type lookups.
           `find_by_type`, `incoming`, `outgoing`, `reachable` go from O(n)
           scans to O(1) hash lookups + maintained adjacency. (D52; review
           item #3.)

- d2a6694  refactor(reconcile): rule-based reconciliation via
           `ReconciliationRule` Protocol. `reconcile_rules.py` extracts
           the policy hub into five independent rules (`VersionDriftRule`,
           `RangeViolationRule`, `LinkageMismatchRule`,
           `PresenceUndeclaredRule`, `CrossDistroRule`), a ResolutionGroup
           combiner, and `DEFAULT_RULES`. Exported from
           `albs_graph.provenance`. +6 tests. (D51; review item #4.)

- 3d27cce  refactor(identity): ArtifactIdentity composes `RpmNevra` into
           the RPM PURL builder. (D50; review item #2.)

- d001ef2  refactor(identity): central `RpmNevra` value object. Dedups
           NEVRA parsing previously scattered across albs / sbom / dnf /
           rpmgraph / remote / payload. (D49; review item #2.)

- a27fe52  feat(reconcile): cross-distro as orthogonal context issue +
           el10 tour. Build `.elN` vs dep `.elM` (N!=M) is a `ContextIssue`,
           not an `Agreement` verdict; coverage policy excludes
           `cross_distro` from the resolution axis. `example--tour.sh`
           default BUILD_ID retargeted 17812 -> 57810 (the el9-build-on-
           el10-host motivating case). +4 tests; suite 194. (D48; review
           item #5.)

----------------------------------------------------------------------
2026-05-26
----------------------------------------------------------------------

- 84f38c5  fix(native): arch-scoped dnf + rpmgraph matching. `dnf
           repoquery` queries the bare name, so on a multi-arch graph
           every arch node inherited the host arch's resolution; now
           scoped to `name.arch`. `rpmgraph` indexed `name -> node` with
           `setdefault` so only the first arch variant got the claims;
           now indexes `name -> [nodes]` and attaches to every arch
           variant. +2 tests. (D47/F8.)

- de440e6  feat(source): per-source `checkout-source` / `source-evidence`
           selector. Batch builds have many sources; the previous code
           always used the build's representative source. `--package`
           resolves the per-task repo+commit (same attribution as the
           graph builder) and `dataclasses.replace` retargets the
           metadata. +1 test. (D47/F6.)

- d09d279  fix(sbom): NEVRA-exact build-SBOM matching. `enrich_graph_with_
           build_sbom` keyed by `(name, arch)` first-wins -- a merged
           graph with two versions of one package attached the *first*
           same-name component's CPE to every match. Now keys by
           `(name, version-release, arch)`. (D47/F3.)

- c8a1488  fix(identity): split vendor-asserted from NVD-verified CPE.
           Vendor SBOM and NVD dictionary verification both establish
           identity but `cpe_status` now distinguishes them (`vendor_
           asserted` vs `verified`); `identity_strength` reports the
           split so a 1.00 axis stays honest about how it was reached.
           (D46/F4.)

- ee72d34  fix: external review scope-boundary findings (Phase 0 + F1).
           F1 = `artifacts_from_source` reimplemented to use per-task
           attribution (`source_to_artifact_path`) rather than
           `graph.reachable`, so a batch build's per-task source linkage
           is precise. (D45/Phase 0 + F1.)

----------------------------------------------------------------------
Earlier history
----------------------------------------------------------------------

D1-D44 (and the original `main` branch commits) are not duplicated here;
their architectural choices are captured in `docs/decisions.md` and the
corresponding code landed before this changelog was instituted. Use
`git log main --oneline` to walk them chronologically if needed.
