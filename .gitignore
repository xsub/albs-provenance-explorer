__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
dist/
build/
*.egg-info/

# Coverage artifacts (pytest --cov, CI Codecov upload).
.coverage
.coverage.*
coverage.xml
htmlcov/

# Local demo outputs generated outside the canonical examples/ paths.
/build-17812*.svg
/nginx-core-x86_64-trust.*
examples/demo-nginx-core/nginx-core_x86_64-trust.*
/investigation-session.json
/investigation-bundle.json
/investigation-report.html
examples/live-build-17812/*.albs.json
# ...except the build-17812 raw metadata: it is the build_analysis test fixture
# (build 17812 is a finished, immutable build, so the data is stable), so commit
# it rather than depend on a locally-generated cache that a clean checkout lacks.
!examples/live-build-17812/build-17812.albs.json
